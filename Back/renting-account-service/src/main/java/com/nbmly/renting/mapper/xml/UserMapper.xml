<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nbmly.renting.mapper.UserMapper">
    <select id="lookupPowers" resultType="string">
        SELECT
            power_scope
        FROM
            USER u,POWER p
        WHERE
            u.account_id = #{accountId} AND p.power_id = u.power_id;
    </select>

    <insert id="powerRegister">
        INSERT INTO
            user
        VALUES
            (NULL,#{accountId},1003);
    </insert>

    <update id="revisePower">
        UPDATE
            user
        SET
            power_id=1002
        WHERE
            account_id = #{accountId};
    </update>

    <update id="reviseUserPower">
        UPDATE
            user
        SET
            power_id=#{powerId}
        WHERE
            account_id = #{accountId};
    </update>
</mapper>